import styled from "styled-components";

const FromToControl = ({ ...props }) => {
  return <FromToControlContainer {...props}></FromToControlContainer>;
};

const FromToControlContainer = styled.div``;

export default FromToControl;
